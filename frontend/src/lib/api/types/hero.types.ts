export interface IHeroSlideData {
	id: string;
	background_image?: { url: string; mime: string };
	small_image?: { url: string; mime: string };
	title: string;
	button_title: string;
	description: string;
}
