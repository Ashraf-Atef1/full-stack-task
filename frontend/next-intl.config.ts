const config = {
	locales: ["en", "ar"], // Add all supported locales
	defaultLocale: "en", // Set your default locale
	localePrefix: "as-needed", // Optional: Can be 'always' | 'as-needed' | 'never'
};

export default config;
